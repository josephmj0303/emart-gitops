apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: node-api
  template:
    metadata:
      labels:
        app: node-api
    spec:
      restartPolicy: Always
      containers:
        - name: node-api
          image: josephmj0303/emart-node-api:3a05034
          imagePullPolicy: Always
          ports:
            - containerPort: 5000

          env:
            - name: MONGO_URL
              value: mongodb://mongo:27017/epoc

          readinessProbe:
            httpGet:
              path: /
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /
              port: 5000
            initialDelaySeconds: 20
            periodSeconds: 10
